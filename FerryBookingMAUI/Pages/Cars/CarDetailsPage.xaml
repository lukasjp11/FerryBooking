<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FerryBookingMAUI.Pages.CarDetailsPage">
    <StackLayout Padding="10">
        <Label Text="Car Details" FontSize="Large" HorizontalOptions="Center" />

        <Label Text="Ferry" FontSize="Medium" />
        <Label Text="{Binding Car.Ferry.Name}" FontSize="Medium" />

        <Label Text="Guests" FontSize="Medium" />
        <CollectionView ItemsSource="{Binding Car.Guests}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout>
                        <Label Text="{Binding Name}" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Text="Edit" Command="{Binding EditCommand}" />
        <Button Text="Delete" Command="{Binding DeleteCommand}" />
    </StackLayout>
</ContentPage>